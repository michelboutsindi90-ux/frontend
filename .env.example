# Leave unset for local dev — the API base URL is derived automatically from
# the page's own hostname (works for both localhost and LAN/mobile access).
# Set it only for a real deployment where frontend and backend are on
# different domains.
# VITE_API_BASE_URL=https://api.example.com
